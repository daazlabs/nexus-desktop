node_modules/
dist/
dist-electron/
src/renderer/dist/
.env
*.log
# OAuth client JSONs written by CI from repo secrets (release.yml) and, if you
# want, locally for testing a packaged build. Never committed.
build-oauth/
